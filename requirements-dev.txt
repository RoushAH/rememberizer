# CI gates: pinned exactly. Black and Flake8 change their rules between
# releases, and an unpinned range means a green PR can turn red with no code
# change. Bump these deliberately, reformatting in the same commit.
black==26.5.1
flake8==7.3.0

pytest>=7.4.0
pytest-flask>=1.3.0
pylint>=3.0.0
ruff>=0.1.0
